local dashboard = require("ui/apps/dashboard")
local peripherals_app = require("ui/apps/peripherals_app")
local organize_app = require("ui/apps/organize_app")
local settings_app = require("ui/apps/settings_app")

return {
    dashboard,
    peripherals_app,
    organize_app,
    settings_app,
}
